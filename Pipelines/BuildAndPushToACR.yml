name : NetCore-ProductMicroservice-CI-CD

trigger:
  branches:  
    include:
    - master
    - ProductMicroservice-DotNET-Core-Master/*

resources:
- repo: self

variables:
  buildConfiguration: 'Release'     
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: 'c1a56e1b-03cf-41ea-83c7-c8a713bf8628'
  imageRepository: 'mehzanproductmicroservicedotnetcoremaster'
  containerRegistry: 'mehzancontainerregistry.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/ProductMicroservice/ProductMicroservice/Dockerfile'
  tag: '$(Build.BuildId)'

  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
